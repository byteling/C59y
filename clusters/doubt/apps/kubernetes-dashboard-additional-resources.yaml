apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubernetes-dashboard-additional-resources
  namespace: argocd
  annotations:
    # ensure this app is applied before the helm app
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: project
  source:
    repoURL: https://github.com/byteling/C59y
    path: apps/kubernetes-dashboard-additional-resources
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd # the Application resource lives in argocd namespace; it applies the Namespace manifest to the cluster
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
